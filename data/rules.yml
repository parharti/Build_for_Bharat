version: "3.1"

rules:
  # -----------------------------
  # 🌧️ RAINFALL INTELLIGENCE RULES
  # -----------------------------

  - rule: Handle rainfall comparison
    steps:
      - intent: compare_rainfall
      - action: action_smart_rainfall

  - rule: Handle rainfall summary
    steps:
      - intent: rainfall_summary
      - action: action_smart_rainfall

  - rule: Handle rainfall trend analysis
    steps:
      - intent: rainfall_trend
      - action: action_smart_rainfall

  - rule: Handle rainfall prediction
    steps:
      - intent: predict_rainfall
      - action: action_smart_rainfall

  - rule: Handle rainfall extremes
    steps:
      - intent: rainfall_extremes
      - action: action_smart_rainfall

  - rule: Handle seasonal rainfall
    steps:
      - intent: rainfall_seasonal
      - action: action_smart_rainfall

  - rule: Handle general rainfall queries
    steps:
      - intent: rainfall_general
      - action: action_smart_rainfall

  # -----------------------------
  # 🤖 BOT INFORMATION RULES
  # -----------------------------
  - rule: Handle bot info
    steps:
      - intent: bot_info
      - action: utter_bot_info

  - rule: Handle source questions
    steps:
      - intent: ask_source
      - action: utter_ask_source

  - rule: Handle greetings
    steps:
      - intent: greet
      - action: utter_greet

  - rule: Handle goodbyes
    steps:
      - intent: goodbye
      - action: utter_goodbye

  # -----------------------------
  # 🧩 FALLBACK RULE
  # -----------------------------
  - rule: Fallback rule
    steps:
      - intent: nlu_fallback
      - action: utter_fallback

  - rule: Handle complex agri queries
    steps:
      - intent: complex_agri_query
      - action: action_smart_agri_insight
